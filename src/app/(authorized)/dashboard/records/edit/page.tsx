import { Breadcrumbs, Container, EditForm } from '@/components';

const Page = () => {
  return (
    <Container className='flex items-center justify-center py-12'>
      <EditForm />
    </Container>
  );
};

export default Page;
