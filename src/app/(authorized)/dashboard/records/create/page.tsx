import { Container, CreateForm } from '@/components';

const Page = () => {
  return (
    <Container className='flex items-center justify-center py-12'>
      <CreateForm />
    </Container>
  );
};

export default Page;
