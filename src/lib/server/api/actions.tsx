'use server';

export const createRecord = async (prevState: any, formData: FormData) => {};

export const updateRecord = async (id: string, prevState: any, formData: FormData) => {};

export const deleteRecord = async (id: string) => {};

export const updateUserData = async (id: string) => {};
